const prisma = require('../config/prisma');

// Paginated Lists Services (APIs 46-50)
exports.getPaginatedRestaurantsService = async (page, limit) => {
    // TODO: Write Prisma query with skip and take for pagination
    // skip = (page - 1) * limit
};

exports.getPaginatedMenuItemsService = async (page, limit) => {
    // TODO: Write Prisma query with skip and take for pagination
};

exports.getPaginatedOrdersService = async (page, limit) => {
    // TODO: Write Prisma query with skip and take for pagination
};

exports.getPaginatedUsersService = async (page, limit) => {
    // TODO: Write Prisma query with skip and take for pagination
};

exports.getPaginatedReviewsService = async (page, limit) => {
    // TODO: Write Prisma query with skip and take for pagination
};